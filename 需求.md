Mod 名称：Super Event Framework
1. 核心概念
一句话描述：一个超事件框架，允许通过XML配置在特定游戏事件发生时，弹出带有图片、音乐的全屏暂停界面。

目标受众：希望为自己的Mod增加叙事冲击力的Mod作者，以及追求沉浸式体验的玩家。

2. 依赖环境
游戏版本：RimWorld 1.6

必需前置Mod：Harmony（v2.3+）

可选组件：HugsLib（用于兼容性辅助，非强制）

3. 功能需求
3.1 核心架构
提供 C# 基础DLL，作为框架的运行时引擎。

支持 纯XML配置 的扩展包作为子Mod。框架需自动发现并加载所有激活Mod中的 <SuperEventDef> 定义。

3.2 触发机制
监听方式：使用 Harmony 补丁拦截 LetterStack.ReceiveLetter 方法。

触发逻辑：当补丁捕获到信件时，根据信件的 defName 或 label，在 Dictionary<string, List<SuperEventDef>> 中进行O(1)查找。如匹配成功，则按玩家选择（或默认后加载优先）决定触发哪个超事件。

性能要求：运行时查找开销需可忽略不计（仅在事件触发时执行）。

3.3 视觉与音频表现
界面：弹出一个全屏或大型居中窗口 Dialog_SuperEvent。

内容：窗口需显示XML中定义的图片（支持PNG/JPG）、背景音乐（支持OGG/WAV）、标题和描述文字。

行为：窗口弹出时，游戏必须自动暂停。关闭窗口后，游戏恢复。

BGM行为：

窗口关闭时，BGM默认停止。

若玩家在Mod设置中关闭“BGM随界面关闭”选项，则BGM继续播放直至自然结束。

资源加载：图片和音乐应采用懒加载策略，仅在触发时加载并缓存，避免启动时占用大量内存。

3.4 持久化与玩家控制
触发模式：需在XML中为每个事件定义默认的 <triggerMode>，支持三种模式：

Unlimited：每次满足条件都可触发。

PerSaveOnce：仅在一个存档内触发一次。

GlobalOnce：在所有存档中全局永久仅触发一次。

存储机制：

PerSaveOnce 状态保存于 GameComponent，跟随存档文件。

GlobalOnce 状态保存于 ModSettings，写入本地配置文件。

玩家对触发模式的修改（覆盖默认值）需保存到 ModSettings。

玩家对同一事件内多个超事件的手动选择需保存到 ModSettings。

Mod设置界面：详见第5节。

3.5 冲突处理与事件优先级
事件（Event）：由 triggerEventId 唯一定义。一个事件可对应多个超事件（来自不同子Mod或同一子Mod的不同配置）。

互斥规则：同一个事件内的多个超事件是互斥的，同时只有一个能被触发。

默认选择逻辑：若玩家未手动选择，框架采用后加载优先规则（即Mod加载顺序中排在最后的生效）。

玩家覆写：玩家可在Mod设置界面中，为同一个事件内的任意超事件勾选“选择”按钮，覆盖默认行为。

4. 数据设计（XML结构）
4.1 核心 Def（由框架定义，写在框架本体中）
xml
<SuperEventDef>
  <defName>唯一标识符</defName>
  <triggerEventId>监听的信件DefName</triggerEventId>
  <displayName>显示标题</displayName>
  <description>显示描述</description>
  <imagePath>UI/Events/图片路径</imagePath>
  <bgmPath>Audio/音乐路径</bgmPath>
  <requiredMod>可选：依赖的Mod PackageId</requiredMod>
  <defaultTriggerMode>Unlimited / PerSaveOnce / GlobalOnce</defaultTriggerMode>
  <!-- 注意：玩家可以在Mod设置中覆盖defaultTriggerMode -->
</SuperEventDef>
4.2 本地化文件结构（Languages文件夹）
text
Languages/
├── English/
│   └── Keyed/
│       └── SuperEvent_Strings.xml
├── ChineseSimplified/
│   └── Keyed/
│       └── SuperEvent_Strings.xml
└── ChineseTraditional/
    └── Keyed/
        └── SuperEvent_Strings.xml
本地化Key示例：

xml
<LanguageData>
  <!-- 界面通用文本 -->
  <SuperEvent_Settings_Title>超事件框架设置</SuperEvent_Settings_Title>
  <SuperEvent_EventList>事件列表</SuperEvent_EventList>
  <SuperEvent_TriggeredInSave>存档已触发</SuperEvent_TriggeredInSave>
  <SuperEvent_TriggeredGlobal>全局已触发</SuperEvent_TriggeredGlobal>
  <SuperEvent_Mode>触发模式</SuperEvent_Mode>
  
  <!-- 触发模式文本 -->
  <SuperEvent_Mode_Unlimited>一直触发</SuperEvent_Mode_Unlimited>
  <SuperEvent_Mode_PerSaveOnce>仅本存档一次</SuperEvent_Mode_PerSaveOnce>
  <SuperEvent_Mode_GlobalOnce>全局仅一次</SuperEvent_Mode_GlobalOnce>
  
  <!-- 按钮文本 -->
  <SuperEvent_ClearSaveRecord>清除本存档记录</SuperEvent_ClearSaveRecord>
  <SuperEvent_ClearGlobalRecord>清除全局记录</SuperEvent_ClearGlobalRecord>
  <SuperEvent_ConfirmClear>确认清除？此操作不可撤销。</SuperEvent_ConfirmClear>
</LanguageData>
5. Mod设置界面详细设计（核心交互）
5.1 总体布局
采用二级折叠列表结构，每个事件（Event）为一个一级条目，可展开显示其下的所有超事件。
事件和超事件显示需要事件所在的dlc或者mod存在且超事件的子mod存在才显示且激活
如金色魔方事件，需要异象dlc，则需要同时存在异象dlc和对应超事件子mod

5.2 一级条目（事件级）
每个事件行包含以下元素：

列	内容	说明
1	事件标识 + 显示名	格式：[triggerEventId] + 当前语言的 <displayName>
2	存档触发状态	按钮形式：显示✓（已触发）或✗（未触发）。点击可翻转状态。仅对PerSaveOnce和GlobalOnce模式有效。
3	全局触发状态	按钮形式：同上。仅对GlobalOnce模式显示。
4	触发模式	按钮形式：当前模式（一直触发/本档一次/全局一次）。点击可循环切换。切换时自动更新事件下所有超事件的触发逻辑。
5	展开三角	▶/▼，控制二级列表的展开/收起。
5.3 二级列表（超事件级）
点击展开后，显示该事件下所有已注册的超事件，每个超事件行包含以下元素：

列	内容	说明
1	超事件标题	使用当前语言的 <displayName>
2	来源子Mod	显示该超事件所属的Mod名称（modContentPack.Name）
3	是否选择	单选按钮/互斥按钮。同一事件内只能有一个超事件被选中。✓表示被选中，其他显示✗。点击后覆写默认选择。支持“清空选择”回到默认规则。
4	测试触发按钮	“▶ 测试”按钮。点击后不经过触发记录检查，直接播放该超事件效果（用于预览，不计入触发状态）。
5.4 额外全局设置
在设置界面顶部或底部增加以下控件：

控件	说明
BGM随界面关闭	复选框。勾选（默认）：窗口关闭时BGM立即停止；不勾选：BGM继续播放直至自然结束。
清除本存档记录	按钮。点击后清除当前存档中所有PerSaveOnce事件的触发记录（需确认弹窗）。
清除全局记录	按钮。点击后清除所有全局GlobalOnce事件的触发记录（需确认弹窗）。
5.5 界面行为要求
玩家对触发模式、超事件选择、BGM选项的修改需立即保存到ModSettings。

玩家修改“是否选择”时，应自动反选同事件内的其他超事件。

测试触发时，即使游戏正在运行，也应临时暂停并播放，播放完毕后恢复正常（不改变游戏内任何状态）。

所有文本需通过本地化Key加载，支持英文、简体中文、繁体中文。

6. 无关需求（明确边界）
此Mod不负责：为其他Mod（如ICBM）自动生成事件配置。作者或用户需自行编写XML，框架仅负责读取和触发。

此Mod不是编辑器：不提供游戏内的可视化超事件编辑器。

7. 测试用例
编号	测试场景	预期结果
1	无任何子Mod启动	游戏正常启动，无报错，设置界面为空或显示提示
2	安装单子Mod，触发对应事件	弹窗正确显示图片/音乐
3	同一事件有2个超事件（ModA后加载）	默认触发ModB因后加载优先规则应被忽略？其实后加载的ModB应生效。玩家可在UI中手动切换到ModA
4	全局唯一事件触发两次	第二次不弹窗
5	在设置界面清除本档记录	同一存档中可再次触发该事件
6	切换触发模式	模式变更后，触发行为立即按新模式执行
7	测试触发按钮	弹窗出现，但触发记录状态不变
8	关闭BGM随界面关闭选项	关闭窗口后BGM继续播放
9	切换游戏语言	界面文本、事件名、超事件标题正确切换为对应语言
8. 文件与目录结构（建议）
text
SuperEventFramework/              # 核心Mod
├── About/
│   └── About.xml                 # Mod信息，声明依赖Harmony
├── Defs/
│   └── SuperEventDef.xml         # 定义抽象父类
├── Source/                       # C#源码
│   ├── SuperEventMod.cs          # Mod入口与设置界面
│   ├── SuperEventDef.cs          # Def数据结构
│   ├── SuperEventManager.cs      # 核心管理器（字典+触发逻辑）
│   ├── SuperEventGameComp.cs     # 存档级存储
│   ├── SuperEventModSettings.cs  # 全局存储+玩家偏好
│   ├── Dialog_SuperEvent.cs      # 弹窗UI
│   ├── HarmonyPatches.cs         # 监听补丁
│   └── SuperEventCache.cs        # 懒加载缓存
├── Languages/                    # 本地化
│   ├── English/
│   ├── ChineseSimplified/
│   └── ChineseTraditional/
└── Textures/                     # 默认资源（占位图）
text
SuperEvent_Example/               # 示例子Mod（纯XML）
├── About/
│   └── About.xml                 # 依赖核心Mod
├── Defs/SuperEvents/
│   ├── NukeExample.xml
│   └── RaidExample.xml
├── Textures/Events/
└── Audio/Events/